language: python

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

install:
  - pip install pip --upgrade
  - pip install PySide2 pytest pytest-qt flake8

jobs:
  include:
    - stage: lint
      script: flake8 ninja_ide
    - stage: tests
      script: python run_test.py

# before_script: rm --recursive --force --verbose *.py[cod]

# script: python run_test.py
